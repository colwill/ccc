# lib.rs.md (20260820-07-57-23) UTC
# source: src/lib.rs [rust]
# modules
    - coverage
    - extract
    - externals
    - html
    - insights
    - languages
    - model
    - naming
    - render
    - scan
    - serve
    - changes
    - tokenize
# imports
    - L17@pub scan (check, scan, Change, ChangeKind, CheckReport, ScanReport)
    - L18@pub serve (serve, ServeOptions)
    - L19@pub changes (init_config, changes, ChangesOptions, ChangesReport)
    - L20@pub externals (ExternalRepo, ExternalService, Surface)
    - L21@pub tokenize (tokenize, Encoding, TokenCache, TokenizeReport)
# const
# funcs
# refs
# note
